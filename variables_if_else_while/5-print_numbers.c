#include <stdio.h>
/**
 * main - print a string
 * Return: return 0 success
 */
int main(void)
{
	int rawr;
	for (rawr = 0; rawr <= 9; rawr++)
	{
		printf("%d", rawr);
	}
	putchar('\n');
	return (0);
}
