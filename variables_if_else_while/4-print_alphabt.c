#include <stdio.h>
/**
 * main - prints a string
 * Return: return (0) Passed
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		if ( ch == e ; ch == q)
			continue
		putchar(ch);
	}
	putchar('\n');
	return (0);
}
