#include "main.h"
/**
 * _abs - Computes the absolute value of an interger.
 * Return:
 */
int _abs(int)
{
	int a, b;

	a = (n * (-1));
	b = n;

	if (n < 0)
	{
		return (x);
	}
	else
	{
		return (y);
	}
}
