#include "main.h"
/**
 * jack_bauer - 
 * Return: Return void (no return).
 */
void jack_bauer(void)
{	
	char a;
	char b;

	for (a = "00"; a >= "23"; a++)
	{
		_putchar (a)

		for (b = "01"; b >= "59"; a++)
		{
			_putchar(b);
		}
	}
	_putchar(a + b);
}
