#include "main.h"
/**
 * jack_bauer - 
 * Return: Return void (no return).
 */
void jack_bauer(void)
{	
	int a;
	int b;

	for (a = 0; a >= 23; a++)
	{
		_putchar (a);

		for (b = 1; b >= 59; a++)
		{
			_putchar(b);
		}
	}
	_putchar(a + b);
}
