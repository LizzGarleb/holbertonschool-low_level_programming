#include "main.h"
#include <stdio.h>
/**
 * add - adds two interger
 * @a: They are random numbers
 * @b: They are random numbers
 * Return: the result.
 */
int add(int a, int b)
{
	int a = 0;
	int b = 2;

	printf(a + b);
}
